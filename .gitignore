# Legacy SQLDash code + third-party reference (kept locally, never published)
legacy/

# New SQLDash system — local dev artifacts
lake/local/
**/inbox/
**/*.ducklake
**/*.ducklake.wal

# Node / TypeScript
node_modules/
dist/
*.tsbuildinfo

# .NET build output (rebuilt via `dotnet build` / `dotnet publish` — not committed)
bin/
obj/
collector/runtime/lib/
bench/dotnet/publish/

# bench scorecards are reproducible run output
bench/results/*.csv

# Logs / temp
*.log
.env
.env.local
