{
  "name": "sqldash-writer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SQLDash DuckLake writer/ingester — the sole DuckLake committer. Reads inbox Parquet, enforces the schema contract, appends to DuckLake.",
  "scripts": {
    "ingest": "tsx src/ingest.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@duckdb/node-api": "1.5.3-r.2"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "@types/node": "^22.10.2"
  }
}
